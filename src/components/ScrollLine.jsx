const ScrollLine = () => {
    return (
        <div style={{
            width: '100%',
            height: '2px',
            background: 'rgba(255, 255, 255, 0.15)',
        }} />
    );
};

export default ScrollLine;
